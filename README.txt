Spengler-DIGITAL V45 – installierbare PWA

Inhalt:
- index.html = aktuelle V45
- manifest.webmanifest = Installationsdaten
- sw.js = Update-/Offline-Shell
- icons/ = App-Symbole

Wichtig: Für die Installation muss die App über HTTPS (z.B. GitHub Pages oder einen anderen HTTPS-Webserver) aufgerufen werden.

Updates: Für V46 wird index.html + CACHE-Version aktualisiert und neu veröffentlicht. Die Benutzer müssen die App normalerweise nicht neu installieren.
